


Employee
-
emp_no PK VARCHAR(30),
emp_title VARCHAR(30),
birth_date date,
first_name VARCHAR(30),
last_name VARCHAR(30),
sex VARCHAR(30),
hire_date date

Salaries
-
emp_no PK VARCHAR(30), FK - Employee.emp_no
salary VARCHAR(30)


Titles
-
title_id PK VARCHAR(30), FK - Employee.emp_title
title VARCHAR(30),

Departments
-
dept_no PK VARCHAR(30), FK - Dept_emp.dept_no
dept_name VARCHAR(30),

Dept_emp
-
emp_no PK VARCHAR(30), FK - Employee.emp_no
dept_no VARCHAR(30),

Dept_manager
-
dept_no PK VARCHAR(30), FK - Departments.dept_no
emp_no VARCHAR(30), FK - Employee.emp_no